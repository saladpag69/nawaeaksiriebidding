[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install --with-deps chromium"
]

[start]
cmd = "gunicorn web_app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --threads 4"
